import { defaultValue, eventDefaultValue } from './defaultValue';
import { handleFunction } from './eventHandler';
import { generateRandomNumber } from './generateRandomNumber';

export { defaultValue, eventDefaultValue, generateRandomNumber, handleFunction };
