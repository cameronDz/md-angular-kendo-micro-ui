import { AnalyticButtonTrackerDirective } from './analytic-button-tracker.directive';

describe('AnalyticButtonTrackerDirective', () => {
    it('should create an instance', () => {
        const directive = new AnalyticButtonTrackerDirective(null);
        expect(directive).toBeTruthy();
    });
});
