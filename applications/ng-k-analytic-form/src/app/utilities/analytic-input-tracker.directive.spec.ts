import { AnalyticInputTrackerDirective } from './analytic-input-tracker.directive';

describe('AnalyticInputTrackerDirective', () => {
    it('should create an instance', () => {
        const directive = new AnalyticInputTrackerDirective(null);
        expect(directive).toBeTruthy();
    });
});
